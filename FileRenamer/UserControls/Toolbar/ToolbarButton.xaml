<UserControl
	x:Class="FileRenamer.UserControls.Toolbar.ToolbarButton"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" >

	<UserControl.Resources>
		<StaticResource ResourceKey="MediumToolbarButtonStyle" x:Key="ToolbarItemSize.Medium" />
	</UserControl.Resources>

	<ContentControl DataContext="{x:Bind Command, Mode=TwoWay}" Style="{StaticResource ToolbarItemSize.Medium}" />

</UserControl>